import { Container, Typography } from "@mui/material";

export default function HomePage() {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Inicio
            </Typography>
            <Typography>Bienvenido a XGP Photo Page</Typography>
        </Container>
    );
}
